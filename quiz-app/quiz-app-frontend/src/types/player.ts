export interface Player {
    id: number
    name: String
    email: String
    password: String
    rating: Number
    points: Number
    totalGames: Number
    wonGames: Number
    isAdmin: Boolean
}